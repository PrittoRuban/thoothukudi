/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ["assets.aceternity.com", "images.unsplash.com"], // Allow this external image domain
    },
  };
  
  export default nextConfig;
  